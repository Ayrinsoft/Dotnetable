@using JsonSerializer = global::System.Text.Json.JsonSerializer
@inject IStorageSettingService StorageService
@inject ISnackbar Snackbar

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Cloud" Class="me-2" />
            @(_isNew ? "Add Storage" : "Edit Storage")
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudForm @ref="_form">
            <MudGrid Spacing="3">
                <MudItem xs="12" sm="6">
                    <MudSelect T="StorageProviderType" @bind-Value="_provider" Label="Provider"
                               Variant="Variant.Outlined" Disabled="@(!_isNew)">
                        <MudSelectItem T="StorageProviderType" Value="StorageProviderType.Arvan">ArvanCloud (S3)</MudSelectItem>
                        <MudSelectItem T="StorageProviderType" Value="StorageProviderType.Dropbox">Dropbox</MudSelectItem>
                    </MudSelect>
                </MudItem>
                <MudItem xs="12" sm="6" Class="d-flex align-center">
                    <MudSwitch T="bool" @bind-Value="_active" Color="Color.Success" Label="Active" />
                </MudItem>

                @if (_provider == StorageProviderType.Arvan)
                {
                    <MudItem xs="12" sm="6">
                        <MudTextField @bind-Value="_arvan.Endpoint" Label="Endpoint" Variant="Variant.Outlined"
                                      Placeholder="https://s3.ir-thr-at1.arvanstorage.ir" Required="true" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField @bind-Value="_arvan.BucketName" Label="Bucket" Variant="Variant.Outlined" Required="true" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField @bind-Value="_arvan.AccessKey" Label="Access Key" Variant="Variant.Outlined" Required="true" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField @bind-Value="_arvan.SecretKey" Label="Secret Key" Variant="Variant.Outlined"
                                      InputType="InputType.Password" Required="true" />
                    </MudItem>
                    <MudItem xs="12" sm="8">
                        <MudTextField @bind-Value="_arvan.PublicBaseUrl" Label="Public Base URL" Variant="Variant.Outlined"
                                      Placeholder="https://my-bucket.s3.ir-thr-at1.arvanstorage.ir" Required="true" />
                    </MudItem>
                    <MudItem xs="12" sm="4">
                        <MudNumericField T="long?" @bind-Value="_arvan.CapacityKB" Label="Capacity (KB)"
                                         Variant="Variant.Outlined" HelperText="Plan size for the quota bar" />
                    </MudItem>
                }
                else
                {
                    <MudItem xs="12">
                        <MudTextField @bind-Value="_dropbox.AccessToken" Label="Access Token" Variant="Variant.Outlined"
                                      InputType="InputType.Password" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField @bind-Value="_dropbox.RootFolder" Label="Root Folder" Variant="Variant.Outlined"
                                      Placeholder="/media" />
                    </MudItem>
                    <MudItem xs="12">
                        <MudExpansionPanels Elevation="0">
                            <MudExpansionPanel Text="Refresh-token flow (optional)">
                                <MudTextField @bind-Value="_dropbox.RefreshToken" Label="Refresh Token" Variant="Variant.Outlined" Class="mb-2" />
                                <MudTextField @bind-Value="_dropbox.AppKey" Label="App Key" Variant="Variant.Outlined" Class="mb-2" />
                                <MudTextField @bind-Value="_dropbox.AppSecret" Label="App Secret" Variant="Variant.Outlined"
                                              InputType="InputType.Password" />
                            </MudExpansionPanel>
                        </MudExpansionPanels>
                    </MudItem>
                }

                <MudItem xs="12" sm="4">
                    <MudNumericField T="long" @bind-Value="_maxFileSizeKB" Label="Max File Size (KB)" Variant="Variant.Outlined" />
                </MudItem>
                <MudItem xs="12" sm="5">
                    <MudTextField @bind-Value="_allowedExtensions" Label="Allowed Extensions" Variant="Variant.Outlined"
                                  Placeholder="jpg,png,pdf  (blank = all)" />
                </MudItem>
                <MudItem xs="12" sm="3" Class="d-flex align-center">
                    <MudSwitch T="bool" @bind-Value="_autoThumbnails" Color="Color.Primary" Label="Thumbnails" />
                </MudItem>
            </MudGrid>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Info" StartIcon="@Icons.Material.Filled.NetworkCheck"
                   OnClick="TestAsync" Disabled="@_busy">Test connection</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="SaveAsync" Disabled="@_busy">Save</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = null!;
    [Parameter] public int WebsiteID { get; set; }
    [Parameter] public WebstieStorageSetting? Setting { get; set; }

    private MudForm _form = null!;
    private bool _busy;
    private bool _isNew => Setting is null;

    private StorageProviderType _provider = StorageProviderType.Arvan;
    private bool _active = true;
    private long _maxFileSizeKB = 10240;
    private string? _allowedExtensions;
    private bool _autoThumbnails = true;

    private ArvanStorageSettings _arvan = new();
    private DropboxStorageSettings _dropbox = new();

    protected override void OnInitialized()
    {
        if (Setting is not null)
        {
            _provider = (StorageProviderType)Setting.StorageProvider;
            _active = Setting.Active;
            _maxFileSizeKB = Setting.MaxFileSizeKB;
            _allowedExtensions = Setting.AllowedExtensions;
            _autoThumbnails = Setting.AutoGenerateThumbnails;

            if (_provider == StorageProviderType.Arvan)
                _arvan = JsonSerializer.Deserialize<ArvanStorageSettings>(Setting.StorageSettingsJSON) ?? new();
            else
                _dropbox = JsonSerializer.Deserialize<DropboxStorageSettings>(Setting.StorageSettingsJSON) ?? new();
        }
    }

    private string BuildJson() => _provider == StorageProviderType.Arvan
        ? JsonSerializer.Serialize(_arvan)
        : JsonSerializer.Serialize(_dropbox);

    private StorageSettingContext BuildContext() => new()
    {
        WebsiteStorageSettingsID = Setting?.WebsiteStorageSettingsID ?? 0,
        WebsiteID = WebsiteID,
        Provider = _provider,
        SettingsJson = BuildJson(),
        MaxFileSizeKB = _maxFileSizeKB,
        AllowedExtensions = _allowedExtensions,
        AutoGenerateThumbnails = _autoThumbnails,
    };

    private async Task TestAsync()
    {
        _busy = true;
        try
        {
            var ok = await StorageService.TestAsync(BuildContext());
            Snackbar.Add(ok ? "Connection succeeded." : "Connection failed — check the credentials.",
                ok ? Severity.Success : Severity.Error);
        }
        catch (Exception ex) { Snackbar.Add($"Test failed: {ex.Message}", Severity.Error); }
        finally { _busy = false; }
    }

    private async Task SaveAsync()
    {
        await _form.ValidateAsync();
        if (!_form.IsValid) return;

        _busy = true;
        try
        {
            if (_isNew)
            {
                await StorageService.CreateAsync(new WebstieStorageSetting
                {
                    WebsiteID = WebsiteID,
                    StorageProvider = (short)_provider,
                    StorageSettingsJSON = BuildJson(),
                    Active = _active,
                    MaxFileSizeKB = _maxFileSizeKB,
                    AllowedExtensions = _allowedExtensions,
                    AutoGenerateThumbnails = _autoThumbnails,
                });
            }
            else
            {
                Setting!.StorageProvider = (short)_provider;
                Setting.StorageSettingsJSON = BuildJson();
                Setting.Active = _active;
                Setting.MaxFileSizeKB = _maxFileSizeKB;
                Setting.AllowedExtensions = _allowedExtensions;
                Setting.AutoGenerateThumbnails = _autoThumbnails;
                await StorageService.UpdateAsync(Setting);
            }
            Snackbar.Add("Storage saved.", Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        catch (Exception ex) { Snackbar.Add($"Save failed: {ex.Message}", Severity.Error); }
        finally { _busy = false; }
    }

    private void Cancel() => MudDialog.Cancel();
}
