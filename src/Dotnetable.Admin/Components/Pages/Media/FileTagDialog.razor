@inject IFileService FileService
@inject ISnackbar Snackbar
@inject IPageLocalizer L

<MudDialog>
    <DialogContent>
        <MudForm @ref="_form">
            <MudTextField @bind-Value="_name" Label="@L["name", "Name"]" Variant="Variant.Outlined"
                          Required="true" Immediate="true" />
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">@L["cancel", "Cancel"]</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SaveAsync" Disabled="@_saving">
            @L["save", "Save"]
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = null!;
    [Parameter] public int WebsiteId { get; set; }
    [Parameter] public FileTag? Tag { get; set; }

    private MudForm _form = null!;
    private bool _saving;
    private string _name = "";

    protected override void OnInitialized()
    {
        if (Tag is not null)
            _name = Tag.Name;
    }

    private void Cancel() => MudDialog.Cancel();

    private async Task SaveAsync()
    {
        await _form.ValidateAsync();
        if (!_form.IsValid || string.IsNullOrWhiteSpace(_name)) return;

        _saving = true;
        try
        {
            if (Tag is null)
                await FileService.CreateTagAsync(WebsiteId, _name.Trim());
            else
                await FileService.RenameTagAsync(Tag.FileTagID, _name.Trim());

            Snackbar.Add(L["saved", "Saved."], Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        catch (Exception ex)
        {
            Snackbar.Add($"{L["save_failed", "Save failed"]}: {ex.Message}", Severity.Error);
        }
        finally { _saving = false; }
    }
}
