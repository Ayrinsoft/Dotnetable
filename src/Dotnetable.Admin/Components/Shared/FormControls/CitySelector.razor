@using Dotnetable.Domain.Entities

<MudAutocomplete T="City" Value="_selected" ValueChanged="OnSelected"
                 SearchFunc="SearchAsync" ToStringFunc="@(c => c?.Title ?? string.Empty)"
                 Label="@Label" Variant="@Variant" Margin="@Margin" Dense="@Dense" Style="@Style" Class="@Class"
                 Required="@Required" RequiredError="@RequiredError" Disabled="@Disabled" ReadOnly="@ReadOnly"
                 Clearable="@Clearable" CoerceText="false" ResetValueOnEmptyText="true"
                 HelperText="@HelperText" />

@code {
    [Parameter] public int? Value { get; set; }
    [Parameter] public EventCallback<int?> ValueChanged { get; set; }
    [Parameter] public List<City> Cities { get; set; } = new();
    [Parameter] public string? Label { get; set; }
    [Parameter] public Variant Variant { get; set; } = Variant.Outlined;
    [Parameter] public Margin Margin { get; set; } = Margin.None;
    [Parameter] public bool Dense { get; set; }
    [Parameter] public string? Style { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter] public bool Required { get; set; }
    [Parameter] public string? RequiredError { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public bool ReadOnly { get; set; }
    [Parameter] public bool Clearable { get; set; } = true;
    [Parameter] public string? HelperText { get; set; }

    private City? _selected;

    protected override void OnParametersSet()
    {
        if (_selected?.CityID != Value)
            _selected = Value is int id ? Cities.FirstOrDefault(c => c.CityID == id) : null;
    }

    private async Task OnSelected(City? city)
    {
        _selected = city;
        var id = city?.CityID;
        if (id != Value)
            await ValueChanged.InvokeAsync(id);
    }

    private Task<IEnumerable<City>> SearchAsync(string value, CancellationToken ct)
    {
        IEnumerable<City> q = Cities.OrderBy(c => c.Title);
        if (!string.IsNullOrWhiteSpace(value))
        {
            var needle = value.Trim();
            q = q.Where(c =>
                c.Title.Contains(needle, StringComparison.OrdinalIgnoreCase)
                || c.CityID.ToString().Contains(needle, StringComparison.OrdinalIgnoreCase));
        }
        return Task.FromResult(q);
    }
}
