{
  "ConnectionStrings": {
    "_comment": "Leave empty on first run — the Setup page collects the database connection and writes it to localsettings.json. SQL Server template: Server=.;Database=Dotnetable;Trusted_Connection=True;TrustServerCertificate=True;",
    "DefaultConnection": ""
  },
  "Database": {
    "Provider": "SqlServer"
  },
  "Branding": {
    "AppName": "Dotnetable"
  },
  "Api": {
    "_comment": "The central API instance (default matches Dotnetable.API's dev launchSettings). Admin pushes cache-invalidation calls here right after a write (see Internal:SyncSecret).",
    "BaseUrl": "https://dtn-api.ayrinsoft.com/"
  },
  "Internal": {
    "_comment": "Shared secret with the API's Internal:SyncSecret. OVERRIDE via env var Internal__SyncSecret in production.",
    "SyncSecret": "dev-only-change-me-cache-sync-secret"
  },
  "Themes": {
    "_comment": "Folder shared with Dotnetable.Web (Themes/). Built-in Default and per-website package folders live here. Override in localsettings.json for your machine.",
    "RootPath": "../Dotnetable.Web/Themes"
  },
  "Cache": {
    "_comment": "Safety-net TTL for cached read services (Menu/Category/Page/Post); tag invalidation on write keeps entries fresh in practice.",
    "DefaultTtlMinutes": 10
  },
  "Hosting": {
    "_comment": "Set true only when this app really sits behind a reverse proxy that rewrites X-Forwarded-For. It makes rate limiting and logging use the real client IP; enabling it without a proxy lets a client forge its own IP.",
    "BehindReverseProxy": false
  },
  "DataProtection": {
    "_comment": "Directory holding the data-protection key ring. Must survive restarts and be SHARED by every instance of this app, or every deploy signs everyone out. Default: App_Data/keys under the content root.",
    "KeyPath": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
