namespace Dotnetable.Domain.Enums;

/// <summary>Values for <see cref="Entities.Settlement.TargetType"/>.</summary>
public enum SettlementTargetType : byte
{
    Vendor = 1,
    Website = 2,
    Supplier = 3,
}
